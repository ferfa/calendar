<Window x:Class="calendar.TaskDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:calendar"
        mc:Ignorable="d"
        Title="Podrobnosti události" Width="650" Height="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1" Width="500" Margin="30">
            <TextBlock Text="Název události"></TextBlock>
            <TextBox Name="TaskNameTB" Text="{Binding Path=EditingTask.Name, Mode=OneTime}" Margin="0, 0, 0, 10"></TextBox>
            <TextBlock Text="Popis"></TextBlock>
            <TextBox Name ="TaskDetailsTB" Text="{Binding Path=EditingTask.Details, Mode=OneTime}"></TextBox>
            <Button Name="OkButton" Content="OK" Width="50" IsDefault="True" Click="OkButton_Click"></Button>
        </StackPanel>
    </Grid>
</Window>
