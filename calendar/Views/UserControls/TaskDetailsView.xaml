<UserControl x:Class="calendar.Views.UserControls.TaskDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:calendar"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Width="500" Margin="30">
            <TextBlock Text="Datum"></TextBlock>
            <WrapPanel>
                <TextBlock Name="TaskDateTB" Width="100" Text="{Binding Path=DisplaySelectedDate, Mode=OneWay}"></TextBlock>
                <Popup Name ="TaskDateCalPopup" PlacementTarget="{Binding ElementName=TaskDateTB}" IsOpen="False">
                    <Calendar Name="TaskDateCal"></Calendar>
                </Popup>
            </WrapPanel>
            <TextBlock Text="Název události"></TextBlock>
            <TextBox Name="TaskNameTB" Margin="0, 0, 0, 10"></TextBox>
            <TextBlock Text="Popis"></TextBlock>
            <TextBox Name="TaskDetailsTB"
                TextWrapping="Wrap"
                AcceptsReturn="True"
                VerticalScrollBarVisibility="Visible"
                Height="200">
            </TextBox>
        </StackPanel>
    </Grid>
</UserControl>
