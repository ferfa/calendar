<Window x:Class="calendar.Views.Windows.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:calendar.Views.Windows"
        xmlns:viewmodels="clr-namespace:calendar.ViewModels"
        mc:Ignorable="d"
        Title="Kalendář" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kalendář" Grid.Row="0" FontSize="36" Margin="5,5,5,5"/>

        <ContentControl Grid.Row="1">
            <ContentPresenter Content="{Binding Path=View}" Margin="20"/>
        </ContentControl>
    </Grid>
</Window>
